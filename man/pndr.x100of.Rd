% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/x100of.R
\name{pndr.x100of}
\alias{pndr.x100of}
\title{Compute a value given its percentage of a total}
\usage{
pndr.x100of(p, t, d = 2)
}
\arguments{
\item{p}{Numeric vector of percentages.}

\item{t}{Numeric scalar or vector representing the total or denominator.
Must be of compatible length with `p` (recycled if length 1).}

\item{d}{Integer; number of decimal digits to round to (default `2`).}
}
\value{
A numeric vector of the same length as `p`, representing the computed values
corresponding to the given percentages.
}
\description{
Converts a percentage (`p`) and a total (`t`) into the corresponding value,
i.e. the inverse operation of \code{pndr.as_x100()}.
Mathematically: \eqn{x = (p * t) / 100}.
}
\examples{
\dontrun{
pndr.x100of(12.5, 200)     # → 25
pndr.x100of(c(10, 20), 50, d = 1)
}

}
\seealso{
[pndr.as_x100()]
}
