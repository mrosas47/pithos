% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc_pckg.R
\name{pndr.doc_pckg}
\alias{pndr.doc_pckg}
\title{Document, build site, and install a package}
\usage{
pndr.doc_pckg(
  path = ".",
  do_site = T,
  do_install = T,
  quiet = F,
  upgrade = "never",
  stop_on_error = F
)
}
\arguments{
\item{path}{Package root directory. Defaults to `'.'`.}

\item{do_site}{Logical; build the site (default `T`).}

\item{do_install}{Logical; install the package (default `T`).}

\item{quiet}{Logical; passed to underlying calls when supported (default `F`).}

\item{upgrade}{Passed to install step; default `'never'`.}

\item{stop_on_error}{Logical; if `T`, stop on first error. If `F`, continue
and attach errors to the returned list (default `F`).}
}
\value{
Invisibly returns a list with elements `document`, `site`, and `install`,
each holding the underlying call result or an `error` object if that step failed.
}
\description{
Runs `devtools::document()`, then builds the pkg site, then installs the
package at `path`. Uses a temporary working directory switch so that
`devtools::build_site()` is called without unsupported arguments.
Falls back to `pkgdown::build_site()` when available.
}
\examples{
\dontrun{
pndr.doc_pckg('.')
pndr.doc_pckg(path = '.', do_site = F, quiet = T)
}

}
